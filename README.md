# textbased-game-prototype
## How to run it
Download all the files and use the command `make` in the same directory. Then type `./main`
## Instruction
Press any of the keys _W_, _A_, _S_, or _D_. Then, You will see the corresponding large character composed of __*__ in the centre of your terminal window. Note that you can press _E_ to exit.
